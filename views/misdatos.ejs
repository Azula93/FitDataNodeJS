<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="Mark Otto, Jacob Thornton, and Bootstrap contributors">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom styles for this template -->
    <link href="/public/css/signin.css" rel="stylesheet">
    <link href="/public/css/styles.css" rel="stylesheet">
    <link href="/public/css/dashboard.css" rel="stylesheet">
    <meta name="generator" content="Hugo 0.87.0">
    <title>Mis Datos</title>
</head>

<body>
    <div class="container-fluid d-flex flex-column min-vh-100">
        <%- include('navbar', { user: user }) %>
        <main class="flex-grow-1 mt-5">
            <div class="container mt-3">
                <section class="p-5">
                    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
                        <h2>Mis Datos</h2>
                        <h2>Bienvenid@:
                            <% if (user) { %>
                                <span class="badge bg-success">
                                    <%= user.nombreUsuario %>
                                </span>
                            <% } %>
                        </h2>
                    </div>
                </section>

                <!-- <table class="table table-striped" id="tabla">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>IMC</th>
                            <th>ICC</th>
                            <th>Gasto Energético</th>
                            <th>Carbohidratos</th>
                            <th>Proteínas</th>
                            <th>Grasas</th>
                            <th>VO2</th>
                            <th>METS</th>
                            <th>Expectativa de Vida</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (datos.length > 0) { %>
                            <% datos.forEach(function(dato) { %>
                            <tr id="data-row">
                                <td>1</td>
                                <td><%= dato.imc %></td>
                                <td><%= dato.icc %></td>
                                <td><%= dato.gasto_energetico %></td>
                                
                                <td>
                                    <button class="btn btn-danger btn-sm delete-btn" data-id="<%= datos[0].id %>">Eliminar</button>
                                </td>
                            </tr>
                            <% }); %>
                            <% } else { %>
                                <tr>
                                    <td colspan="2" class="text-center">No hay datos disponibles.</td>
                                </tr>
                            <% } %>
                    </tbody>
                </table> -->


               
                <h1 class="my-4">Mis Datos</h1>
                <table class="table table-striped" id="misdatos_table">
                    <thead>
                    
                        <tr>
                            <th>IMC</th>
                            <th>ICC</th>
                            <th>Gasto Energético</th>
                            <th>Macronutrientes</th>
                            <th>VO2</th>
                            <th>METS</th>
                            <th>Expect Vida</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (Object.keys(datos).length > 0) { %>
                        <tr>
                            <td><%= datos.imc %></td>
                            <td><%= datos.icc %></td>
                            <td><%= datos.gasto_energetico %></td>
                            <td><%= datos.macro %></td>
                            <td><%= datos.vo2 %></td>
                            <td><%= datos.mets %></td>
                            <td><%= datos.expect_vida %></td>
                            <td>
                                <button class="btn btn-danger btn-eliminar" data-id="<%= datos.user_id  %>">Eliminar</button>
                            </td>
                        </tr>
                        <% } else { %>
                            <tr>
                                <td colspan="8">No hay datos disponibles</td>
                            </tr>
                        <% } %>
                    </tbody>

                </table>
                
               

                <div class="p-3">
                    <a class="btn btn-primary" href="/generar-pdf" target="_blank">Generar PDF</a>
                </div>

                <%- include('footer', { user: user }) %>
            </div>
        </main>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://kit.fontawesome.com/52b9ec387c.js" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="/public/js/delete.js"></script>
</body>

</html>
